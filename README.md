# svgwebcomponent
A library to manage complex, nested SVG objects as if they were web components, including all the percs of a modern JS framework: templates, observables, reactive re-rendering and so on.
